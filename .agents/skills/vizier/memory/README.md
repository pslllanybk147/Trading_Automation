# Vizier runtime memory (PRIVATE)

Real theses, decision log and NAV snapshots live here and are **gitignored**.
Only templates/examples (`EXAMPLE_*`, `.gitkeep`) are committed.
This mirrors how the MCPs gitignore `.env`.
